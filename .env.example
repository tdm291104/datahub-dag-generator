# Default LLM provider: OpenRouter + openai/gpt-5.2 (configured in llm_models.yaml)
OPENROUTER_API_KEY=sk-or-v1-...

# Optional: direct Anthropic key, used with --provider anthropic
# ANTHROPIC_API_KEY=sk-ant-...

# DataHub GMS server URL (default when running locally via docker quickstart)
DATAHUB_SERVER=http://localhost:8080

# Optional: authenticated/remote DataHub token
# DATAHUB_TOKEN=

# Runtime safety limits
# LLM_MODEL_CONFIG=/absolute/path/to/llm_models.yaml
# DATAHUB_MCP_PACKAGE=mcp-server-datahub@0.6.0
# MAX_AGENT_TURNS=20
# MAX_TOOL_RESULT_CHARS=200000
# MAX_LINEAGE_NODES=200
